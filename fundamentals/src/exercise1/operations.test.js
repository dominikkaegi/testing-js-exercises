const operations = require('./operations')
const {
  add,
  subtract,
  multiply,
  divide
} = operations


test('add operation adds two numbers', () => {

})

test.skip('subtract operation subtracts two numbers', () => {
})

test.skip('multiply operation multiplys two numbers', () => {
})

test.skip('divide operation divides two numbers', () => {
})