// Use the fireEvent from "@testing-library/react"
test.skip("Button fires onClick correctly", () => {
});

// Use the user from "@testing-library/user-event"
test.skip("Button fires onClick correctly", () => {

});
